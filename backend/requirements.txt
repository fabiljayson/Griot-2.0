# --- Core web framework -------------------------------------------------
Django==5.2.4
python-dotenv==1.1.0                       # Load backend/.env in settings
dj-database-url==3.1.2                     # Parse DATABASE_URL (prod Postgres)

# --- Database (prod Postgres) ---------------------------------------------
psycopg[binary]==3.3.4                        # PostgreSQL driver for prod

# --- Production WSGI server -------------------------------------------------
# Unix-only: skipped on Windows dev machines via the platform marker.
gunicorn==26.0.0; sys_platform != 'win32'      # WSGI server (Procfile/render)

# --- REST API (Phase 1) -------------------------------------------------
djangorestframework==3.16.0
djangorestframework-simplejwt==5.5.0          # JWT auth (Phase 2)
django-cors-headers==4.7.0                    # CORS for Flutter web/PWA

# --- Media & images (Phase 1/3) ------------------------------------------
Pillow==11.2.1                                # ImageField support
blurhash-python==1.2.2                        # BlurHash on image upload

# --- QR codes & artifacts (Phase 5) ---------------------------------------
qrcode[pil]==8.0                              # QR code generation
segno==1.6.1                                  # High-quality QR SVG output

# --- AI & notifications (Phase 4) -----------------------------------------
firebase-admin==6.7.0                         # FCM push notifications
requests==2.32.4                              # Outbound calls (Luma AI, etc.)

# --- Text-to-Speech (Phase 4) ---------------------------------------------
gTTS==2.5.1                                   # Google Text-to-Speech

# --- Observability (Phase 10) ---------------------------------------------
sentry-sdk==2.28.0
