<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security policy for the Griot AI app.

  The backend defaults to plain http://10.0.2.2:8000 for local development
  (see AppConstants.apiBaseUrl). Android 9+ blocks all cleartext traffic by
  default, which silently broke every artifact/story image and API call.
  This config allows cleartext ONLY to local development hosts — all other
  traffic (including production HTTPS) is unaffected. Add a tunnel domain
  here the same way if you debug through ngrok.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
